package net.ollysk.pr.service;

//@SpringBootTest
class UserServiceTest {
/*
  @Autowired
  UserRepo userRepo;
  User user;
  UserWeb userDto;
  UserService userService;
  @Autowired
  UserMapper userMapper;

  @BeforeEach
  void setUp() {
    // System.out.println("JJJ "+userMapper+userRepo);
    //user = User.builder().id(2).email(2+"@example.com").name("User-"+2).isActive(true).build();
    userDto = UserWeb.builder().email(2 + "@example.com").username("User-" + 2).build();
    //userService = new UserServiceImpl(userRepo, null, userMapper, null);
  }

    @AfterEach
    void tearDown() {
    }

    @Test
    void register() {
    }

    @Test
    void disableUser() {
    }

    @Test
    void findUserByEmail() {
    }

    @Test
    void getUserById() {
        //userRepo.save(user);
      userService.register(userDto, "123", null);
        //   String email = userService.getUserById(1L).get().getEmail();
        assertThat(userDto.getEmail()).isEqualTo(userService.getUserById(1L).get().getEmail());
    }*/
}